enum dissonance
{
	onready,
	ondisconnect,
	onerror,
	onjoin,
	onspectate,
	onjoinrequest,
	handlers
}

function rousr_dissonance_create() //rousr_dissonance_create
{
    /*var _application_id = string(argument[0]);
    var _steam_id = (argument_count > 1 ? argument[1] : "");
    if (!variable_global_exists("global.__rousr_dissonance") || !instance_exists(global.__rousr_dissonance))
    {
        with (instance_create_depth(0, 0, 0, rousrDissonance))
            global.__rousr_dissonance = id;
    }
    with (global.__rousr_dissonance)
    {
        Is_dirty = 0;
        Is_ready = 0;
        Last_error_code = undefined;
        Last_error_msg = undefined;
        Handlers = array_create(dissonance.handlers);
        for (var i = 0; i < dissonance.handlers; i++)
            array_set(Handlers, i, [rousr_dissonance_dummy_function, undefined]);
        Avatars = [];
        Num_avatars = 0;
        if (!Discord_initialized)
            Discord_initialized = discord_init(_application_id, _steam_id) == 1;
        if (!Discord_initialized)
        {
            show_debug_message("Discord RPC not initialized!");
            return 0;
        }
    }
    return 1;*/
}

